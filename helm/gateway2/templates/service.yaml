apiVersion: v1
kind: Service
metadata:
  name: gateway2
  labels:
    run: gateway2
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 80
  selector:
    run: gateway2
